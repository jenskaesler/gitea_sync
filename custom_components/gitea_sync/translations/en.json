{
  "config": {
    "step": {
      "user": {
        "title": "Set up Gitea Config Sync",
        "description": "Connect Home Assistant to your Gitea repository.",
        "data": {
          "url": "Gitea URL",
          "token": "Gitea Access Token",
          "owner": "Repository Owner (user or organization)",
          "repo": "Repository Name",
          "branch": "Branch (default: main)"
        }
      },
      "paths": {
        "title": "Paths to Synchronize",
        "description": "Define which files and folders should be synchronized.",
        "data": {
          "include_paths": "Included paths (comma-separated, e.g. *.yaml,automations/)",
          "exclude_paths": "Excluded paths (comma-separated, e.g. secrets.yaml,.storage/)",
          "commit_message_prefix": "Commit message prefix"
        }
      },
      "schedule": {
        "title": "Sync Schedule",
        "description": "When should synchronization run automatically?",
        "data": {
          "auto_sync_interval": "Auto-sync interval in minutes (0 = disabled)",
          "sync_on_start": "Sync on HA startup",
          "watch_files": "Watch files and sync on change"
        }
      }
    },
    "error": {
      "cannot_connect": "Failed to connect to Gitea. Please check URL and token.",
      "invalid_token": "Invalid access token.",
      "repo_not_found": "Repository not found. Please check owner and repository name.",
      "unknown": "Unexpected error."
    },
    "abort": {
      "already_configured": "Gitea Sync is already configured."
    }
  },
  "options": {
    "step": {
      "init": {
        "title": "Gitea Sync Settings",
        "data": {
          "include_paths": "Included paths",
          "exclude_paths": "Excluded paths",
          "auto_sync_interval": "Auto-sync interval (minutes)",
          "sync_on_start": "Sync on startup",
          "watch_files": "Watch for file changes",
          "commit_message_prefix": "Commit message prefix"
        }
      }
    }
  }
}
